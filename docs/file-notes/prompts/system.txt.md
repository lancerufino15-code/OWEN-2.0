# prompts/system.txt

Purpose
- System prompt template for the "Atlas" assistant persona (tone, citations, formatting).

Schema/fields
- Plain text instructions covering style, sourcing, formatting, and citation block format.

Who reads/writes
- Humans (prompt design); not referenced by runtime code.

Notes
- NOTE: As of this repo snapshot, this file is not imported by the worker.
- Content matches `system.txt` at the repo root.
